# 2026-02-21 - Dash App Mode and Example

## Summary

Implemented full Dash support path for Interactive Studio with embedded preview and process lifecycle controls.

## Backend (Tauri / Rust)

- Added managed Python app lifecycle in `src-tauri/src/commands/python.rs`:
  - New commands: `run_python_app`, `stop_python_app`
  - New event: `python-app-ready` with app URL payload
  - Added `PythonProcessManager` managed state to track one active long-running Python app process
  - Added readiness probing on localhost and port auto-selection (starting from 8050, fallback range)
  - Added stdout/stderr streaming for long-running apps and proper `python-exit` emission
- Kept existing one-shot `run_python` behavior for normal scripts
- Wired commands/state in `src-tauri/src/lib.rs`

## Frontend (React / Zustand)

- Extended execution state in `src/store/executionStore.ts`:
  - Added `previewUrl`
  - Added `runningMode` (`script` | `app` | `null`)
  - Added actions `setPreviewUrl` and `setRunningMode`
- Added Tauri wrappers in `src/lib/tauriFS.ts`:
  - `runPythonApp(...)`
  - `stopPythonApp()`
- Updated `src/hooks/useCodeExecution.ts`:
  - Detects Dash-like files and runs app mode via `run_python_app`
  - Handles `python-app-ready` event and switches preview to URL iframe mode
  - Adds stop behavior via `stop_python_app`
  - Preserves script-mode Python output behavior
- Updated `src/hooks/usePreview.ts` to avoid overriding URL preview while app mode is active
- Updated `src/components/preview/PreviewPane.tsx` with URL preview iframe mode and waiting state
- Updated `src/components/editor/EditorPane.tsx` with Stop button + shortcut behavior for running app mode

## Workspace Example

Added `workspace/python-dash/`:
- `app.py` - minimal Dash + Plotly graph app
- `pyproject.toml` - project dependency on `dash`
- `README.md` - run/stop notes and port behavior

## Documentation and Meta

- Updated `README.md` feature list and examples table for Dash support
- Updated `ARCHITECTURE.md` for new command surface and Python app data flow
- Appended latest prompts to `PROMPT.md`
- Created empty `AGENTS.md` (was missing)

## Validation

- `cargo check` (in `src-tauri/`) passed
- `pnpm test` passed: 24 files, 180 tests
- `pnpm build` passed

---

## Update — FastAPI App Mode Support and Example

Added FastAPI support path on top of the existing Python app preview infrastructure.

### Frontend execution detection

- Updated `src/hooks/useCodeExecution.ts`:
  - Added FastAPI web-app detection (FastAPI import + `FastAPI(...)` + `uvicorn.run(...)`)
  - Unified web-app startup path so both Dash and FastAPI run via `run_python_app`
  - Updated startup console message to `Starting Python web app ...`

### Tests

- Updated `tests/unit/hooks/useCodeExecution.test.ts`:
  - Added a new test that verifies FastAPI files trigger `runPythonApp(...)`
  - Generalized test fixture helper to support different project/file names

### Workspace example

Added `workspace/python-fastapi/`:
- `main.py` - FastAPI + Uvicorn app using runtime-injected `HOST` and `PORT`
- `pyproject.toml` - dependencies for `fastapi` and `uvicorn`
- `README.md` - run/stop flow and port behavior notes

### Documentation sync

- Updated `README.md`:
  - Python app preview feature now explicitly mentions Dash/FastAPI
  - Added `python-fastapi` row in example projects table
- Updated `ARCHITECTURE.md`:
  - App mode descriptions now mention Dash/FastAPI detection and preview path
- Appended latest prompt to `PROMPT.md`
